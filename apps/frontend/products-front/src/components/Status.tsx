import CircleIcon from '@mui/icons-material/Circle';

export default function Status(props: { className?: string }) {
  return (
    <div className={props.className}>
      <CircleIcon />
    </div>
  );
}
